version: 3
automerge: true
delete_source_branch_on_merge: true
projects:
  - dir: legacy-production/platform-ops-automation
    name: platform-ops-automation-legacy-production
    apply_requirements: [mergeable, approved]
    autoplan:
      enabled: true
      when_modified: ["**/*.tf", "*.hcl"]
  - dir: plentiau-test/platform-ops-automation
    name: platform-ops-automation-plentiau-test
    apply_requirements: [mergeable, approved]
    autoplan:
      enabled: true
      when_modified: ["**/*.tf", "*.hcl"]
  - dir: plentiau-production/platform-ops-automation
    name: platform-ops-automation-plentiau-production
    apply_requirements: [mergeable, approved]
    autoplan:
      enabled: true
      when_modified: ["**/*.tf", "*.hcl"]
  - dir: legacy-production/DevOps
    name: DevOps-legacy-production
    apply_requirements: [mergeable, approved]
    autoplan:
      enabled: true
      when_modified: ["**/*.tf", "*.hcl"]
  - dir: legacy-production/ProductionMonitor
    name: ProductionMonitor-legacy-production
    apply_requirements: [mergeable, approved]
    autoplan:
      enabled: true
      when_modified: ["**/*.tf", "*.hcl"]
  - dir: legacy-production/ServiceConfiguration
    name: ServiceConfiguration-legacy-production
    apply_requirements: [mergeable, approved]
    autoplan:
      enabled: true
      when_modified: ["**/*.tf", "*.hcl"]
